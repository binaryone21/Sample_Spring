/*
let me = window.AMBoard = {
    startUp : () => {
        me.setVariables();
        me.setEvents();
        me.initialize();
    },
    setVariables : () => {
        me.searchMap = ${result.searchMap};

    },
    setEvents : () => {
        document.addEventListener('keyyp', me.searchEnter);
    },
    initialize : () => {
        me.searchBoardList(1);
    },
    searchEnter : () => {
        if(window.event.keyCode == 13) me.searchBoardList(1);
    },
    searchBoardList : () => {

    }

}
AMBoard.startUp()
*/
